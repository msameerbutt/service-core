#behat.yml
default:
  autoload:
    '': '%paths.base%/tests/Feature/bootstrap'
  suites:
    default:
      paths:
        - '%paths.base%/tests/Feature'
    extensions:
      Behat\MinkExtension:
        base_url: http://www.example.com
        selenium2: ~
